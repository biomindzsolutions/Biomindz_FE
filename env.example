# Environment
VITE_NODE_ENV=development

# Backend API Configuration
# For local development, point to your local backend
VITE_API_BASE_URL=http://localhost:9099

# Proxy Configuration (for local development)
# Vite proxy forwards /api/* requests to this URL
VITE_PROXY_TARGET=http://localhost:9099

# API Prefix
VITE_API_PREFIX=/api
